# Система мониторинга TV каналов

Cистема для мониторинга состояния TV-каналов с веб-интерфейсом в виде интерактивной видеостены, и уведомлениями в телеграм.

<img width="1920" height="1080" alt="image" src="https://github.com/user-attachments/assets/b16bba61-9ae7-4b83-9881-e7ef58885ffa" />

## 📋 Содержание
- [Общее описание](#%EF%B8%8F-общее-описание)
- [Архитектура системы](#%EF%B8%8F-архитектура-системы)
- [Требования](#%EF%B8%8F-требования)
- [Установка и настройка](#-установка-и-настройка)
- [Запуск системы](#-запуск-системы)
- [Использование веб-интерфейса](#-использование-веб-интерфейса)
- [Особенности работы](#-особенности-работы)
- [Мониторинг и диагностика](#-мониторинг-и-диагностика)
- [Масштабирование](#-масштабирование)

## 🏗️ Общее описание

Система представляет собой распределенное решение для мониторинга состояния TV-каналов, состоящее из трех основных компонентов:

- **Веб-интерфейс** (`index.html`) - интерактивная адаптивная видеостена для визуализации состояния каналов
- **Серверный модуль** (`Streamchecker_server.py`) - координация работы клиентов, управление данными и уведомлениями
- **Клиентские модули** (`Streamchecker_clients.py`) - анализ состояния каналов и создание скриншотов

## 🏛️ Архитектура системы

```
┌─────────────────┐    ┌──────────────────┐    ┌──────────────────┐
│   Веб-интерфейс │    │   Сервер         │    │   Клиенты        │
│   (index.html)  │◄──►│   (Python)       │◄──►│   (Python)       │
└─────────────────┘    └──────────────────┘    └──────────────────┘
         │                      │                       │
         │                      │                       │
         ▼                      ▼                       ▼
┌─────────────────┐    ┌──────────────────┐    ┌──────────────────┐
│   Видеостена    │    │   Балансировка   │    │   Проверка       │
│   состояний     │    │   нагрузки       │    │   каналов        │
└─────────────────┘    └──────────────────┘    └──────────────────┘
```

## ⚙️ Требования

### Программное обеспечение:
- **Python 3.7+**
- **Библиотеки Python**:
  ```bash
  pip install opencv-python pillow requests numpy
  ```
- **Веб-браузер** с поддержкой современных стандартов (Chrome, Firefox, Edge), Веб-браузер телевизора, особенно с большим экраном тоже подойдет
- **FFmpeg** (для обработки видео потоков)

## 🚀 Установка и настройка

### Шаг 1: Подготовка файловой структуры
```
monitoring_system/
├── index.html
├── Streamchecker_server.py
├── Streamchecker_clients.py
├── streams.csv
├── channel_states.json (создается автоматически)
└── screenshots/ (создается автоматически)
```

### Шаг 2: Настройка списка каналов (`streams.csv`)
Формат файла:
```csv
number,name,url,double
1,Первый канал,udp://224.1.1.1:5510
2,Россия 1,udp://224.1.1.2:5510,2
```

**Поля:**
- `number` - номер канала
- `name` - название канала
- `url` - URL мультикаст потока
- `double` - режим проверки (0 - пропуск канала, пусто/1 - обычная, 2 - двойная проверка)

### Шаг 3: Настройка Telegram уведомлений (опционально)
В файле `Streamchecker_server.py` измените параметры:
```python
TELEGRAM_BOT_TOKEN = "ваш_токен_бота"
TELEGRAM_CHAT_ID = "ваш_chat_id"
```

### Шаг 4: Настройка прокси (при необходимости)
В файле `Streamchecker_server.py` укажите прокси:
```python
proxies = {
    'http': 'http://ваш_прокси:порт',
    'https': 'http://ваш_прокси:порт'
}
```

## 🎯 Запуск системы

### Запуск сервера:
```bash
python Streamchecker_server.py
```

**Логи при успешном запуске:**
```
[Server] Started on 0.0.0.0:8888
[Server] Local IP: 192.168.1.100
[Server] Monitoring 150 channels
[Server] Waiting for clients...
```

### Запуск клиентов (на одном или нескольких компьютерах):
```bash
# Базовый запуск вместе с сервером
python Streamchecker_clients.py

# С указанием сервера
python Streamchecker_clients.py --host 192.168.1.100 --port 8888
```

**Рекомендуется запускать несколько клиентов для балансировки нагрузки.**

### Запуск веб-интерфейса:
Откройте файл `index.html` в веб-браузере.

## 🖥️ Использование веб-интерфейса

### Основные функции:

1. **Видеостена**:
   - Автоматическая адаптация сетки под размер экрана
   - Цветовая индикация состояния каналов
   - Обновление в реальном времени

2. **Интерактивные элементы**:
   - **Клик по каналу** - открытие увеличенного скриншота телеканала
   - **Кнопка "📋 Список"** - копирование списка неисправных каналов в буфер обмена (работает только на ПК)

### Управление модальным окном:
- **Клик по изображению** - закрытие окна
- **Крестик в углу** - закрытие окна
- **Навигационные миниатюры** - переключение между неисправными каналами
- **Кнопка "Список"** - копирование списка проблемных каналов

## 🔧 Особенности работы

### Режимы проверки каналов (Уведомления в Телеграм):

1. **Пропуск канала** (`double = 0`)

2. **Обычная проверка** (`double = 1 или пусто`):
   - Уведомление при падении канала
   - Немедленное уведомление о восстановлении

3. **Двойная проверка** (`double = 2`):
   - Первое падение: счетчик = 1 (без уведомления)
   - Второе падение: счетчик = 2 (отправка уведомления)
   - Третье падение: счетчик = 3 (закрепление сообщения)
   - Восстановление: сброс счетчика

### Логика работы клиентов:
- Клиенты подключаются к серверу и получают задания
- Каждый клиент проверяет один канал за раз
- При отключении клиента сервер автоматически перераспределяет нагрузку
- Поддерживается неограниченное количество клиентов

## 📊 Мониторинг и диагностика

### Ключевые логи сервера:
```
[Server] Processing result for 1 - Первый канал: ✅ (2.1s) from ('192.168.1.101', 54321)
[Alert] Канал 5 - Пятый канал не отвечает
[Recovery] Канал 5 - Пятый канал восстановлен
```

### Ключевые логи клиента:
```
[Client] Checking channel 1 - Первый канал
[Client] Stream check: ✅ Working (2.1s)
```

### Файлы данных:
- `channel_states.json` - текущее состояние всех каналов
- `screenshots/` - архив скриншотов (сохраняются последние 2 файла на канал)

## 📈 Масштабирование

### Для увеличения скорости проверки:
Запустите дополнительные клиенты на других машинах:
   ```bash
   python Streamchecker_clients.py --host IP_СЕРВЕРА --port 8888
   ```
